@model MongoTodo.Models.Todo

@using (Html.BeginForm("Add", "TodoManager", FormMethod.Post, new { id = "AddTodoForm" , @class = "cmxform"}))
{
    @Html.AntiForgeryToken()
   
    <div class="form-horizontal">
        <h4>Todo</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Titel, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Titel, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Titel, "", new { @class = "text-danger" })
            </div>
            <div class="col-md-10">
                <input type="submit" value="Save" class="btn btn-default"/>
            </div>
        </div>
    </div>
}
<script>
    $(document).ready(function () {
        console.log("Test");
        var form = document.getElementById("AddTodoForm");
        /*
        $("#AddTodoForm").validate();
        $("#AddTodoForm").submit(function (event) {
            var isValid = $this.valid();

            if (!isValid)
                event.preventDefault();
        });
        $('#Titel').rules('add', {
            required: true,
            minlength: 5,
            maxlength: 32,
            messages: {
                required: "Please enter a todo titel",
                minlength: "Titel at least 5 characters",
                maxlength: "Titel maximum length 32 characters"
            }
        });*/
    });
</script>